on: [pull_request]
name: benchmark pull requests
jobs:
  runBenchmark:
    name: run benchmark
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@master
      - uses: rusticstuff/criterion-compare-action@use-main-branch
        with:
          benchName: "benchmark"
          token: ${{ secrets.GITHUB_TOKEN }}
        env:
          RUSTFLAGS: -C target-feature=+avx2
